0.3.0 — Adds P4 variables and bounded control flow: if/else, repeat, wait, random values and captured output. Adds mobile-data, clipboard text, screen-state and dark-mode primitives. Known gaps: resolved LLM capture is not yet supported by Hermes, nested P4 generative delivery remains fail-closed, and natural-language dark-mode drafts have a wire-enum mismatch. The taint policy is now more permissive; shell triggers remain restricted.
