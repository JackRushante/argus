0.3.0 — Rules can now use variables and control flow: if/else, repeat (with a variable count), wait, random numbers, and text capture. New actions: dark mode, mobile-data toggle, copy literal text to clipboard, and a screen on/off condition. The on-device anti-injection guard was relaxed to a more permissive posture (shell commands stay restricted to trusted contacts). Faster rule-compilation model on the self-hosted bridge.
